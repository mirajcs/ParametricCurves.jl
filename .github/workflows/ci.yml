name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        version: ['1.12', '1']
        include:
            - os: ubuntu-latest
              version: nightly  

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}

      # Install Julia dependencies
      - name: Install dependencies
        run: julia --project=. -e 'using Pkg; Pkg.instantiate()'

      # Configure PyCall to use Conda BEFORE loading SymPy
      - name: Configure PyCall to use Conda
        run: |
            julia --project=. -e 'ENV["PYTHON"] = ""; using Pkg; Pkg.build("PyCall")'

      # Install SymPy in Conda (now PyCall is configured)
      - name: Install SymPy via Conda
        run: |
            julia --project=. -e 'using Pkg; Pkg.add("PyCall"); using PyCall; pyimport_conda("sympy", "sympy")'

      # Run tests
      - name: Run tests
        run: julia --project=. -e 'using Pkg; Pkg.test(coverage=true)'